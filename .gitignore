# Flutter Project
# IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.iml
*.ipr
*.iws
.atom/

# OS
.DS_Store
Thumbs.db

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
coverage/
pubspec.lock
app.*.symbols
app.*.map.json
android/app/debug
android/app/profile
android/app/release
*.freezed.dart
*.g.dart
*.gr.dart

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Miscellaneous
*.class
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# Everything in lib/ is regenerated by compose, so it is not tracked -
# EXCEPT main.dart, which is hand-authored app composition and which the
# installer likewise refuses to overwrite. Note 'lib/*' not 'lib/': git
# cannot re-include a file whose parent directory is excluded, so a bare
# 'lib/' rule would make the negation below silently inert.
lib/

# Composed SDK sources and composer scratch - regenerated by compose
.rokct/cache/
.rokct/__pycache__/
